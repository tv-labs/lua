package = "test-project"
version = "1.0-1"

source = {
  url = "."
}

description = {
  summary = "A test rockspec for unit tests",
}

dependencies = {
  "lua >= 5.1",
  "inspect >= 3.0",
  "middleclass >= 4.0",
}
